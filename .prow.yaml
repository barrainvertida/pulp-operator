presubmits:
- name: pull-test-infra-yamllint
  always_run: true
  decorate: true
  skip_report: false
  spec:
    containers:
    - image: quay.io/fedora/fedora
      command:
      - /usr/bin/dnf install -y make
      - /usr/bin/make bundle
